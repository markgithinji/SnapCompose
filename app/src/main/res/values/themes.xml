<resources>

    <style name="Theme.ComposeGallery" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MyApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/snap_app_icon</item>
        <item name="windowSplashScreenAnimationDuration">500</item> <!-- 1 second -->
        <item name="postSplashScreenTheme">@style/Theme.ComposeGallery</item>
    </style>
</resources>
